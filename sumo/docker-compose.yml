services:
  traffic-simulation:
    build: .
    container_name: traffic_simulation
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    environment:
      - PYTHONUNBUFFERED=1
    command: ["python", "simulation/traffic_simulation.py"]